{
  "name": "calculetes-project",
  "version": "1.0.0",
  "description": "A monorepo for the Calculetes project",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "page:dev": "bun --hot packages/page/src/index.tsx"
  }
}
